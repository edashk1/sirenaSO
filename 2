// This program takes a string as input and prints the number of characters in the string

// Prompt the user to enter a string and save it to a variable
const inputString = prompt("Enter a string:");

// Get the length of the input string
const stringLength = inputString.length;

// Print the result to the console
console.log(`The length of the string "${inputString}" is ${stringLength}`);
